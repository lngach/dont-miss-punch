{
  "author": "Nah",
  "license": "MIT",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "start": "node dist/index.ts",
    "build": "mkdir -p dist && cp .env execute.sh dist/ && tsc",
    "prepublishOnly": "npm run build"
  },
  "files": [
    "bin",
    "dist",
    "package.json"
  ],
  "dependencies": {
    "dotenv": "^16.0.0",
    "puppeteer-core": "^13.6.0"
  },
  "devDependencies": {
    "@types/node": "^17.0.25",
    "@types/puppeteer": "^5.4.5"
  }
}
